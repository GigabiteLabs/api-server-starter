docker run -d \
        -p 3000:8080 \
        -v $PWD:/home/app \
        -w /home/app \
        --env "LOG_LEVEL=$LOG_LEVEL" \
        --env "PORT=$PORT" \
        $DOCKER_ACCT_NAME/$DOCKER_APPLICATION_NAME